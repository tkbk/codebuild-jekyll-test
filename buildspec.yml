version: 0.2
           
phases:
  install:
    commands:
      - gem install jekyll
      - gem install bundler
      - gem install minima      
  pre_build:
    commands:
      - jekyll --version
      - bundler --version                  
      - cd codebuild-jekyll-test
  build:
    commands:
      - bundle exec jekyll b

      

